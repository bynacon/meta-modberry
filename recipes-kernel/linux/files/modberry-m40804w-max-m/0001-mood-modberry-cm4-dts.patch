diff --git a/bcm2711-rpi-cm4.dts.orig b/bcm2711-rpi-cm4.dts
index 471d073..5cc379b 100644
--- a/arch/arm/boot/dts/bcm2711-rpi-cm4.dts
+++ b/arch/arm/boot/dts/bcm2711-rpi-cm4.dts
@@ -298,9 +298,10 @@
 		bootargs = "coherent_pool=1M 8250.nr_uarts=1 snd_bcm2835.enable_compat_alsa=0 snd_bcm2835.enable_hdmi=1";
 	};
 
+	//Change serial 0 and 1
 	aliases {
-		serial0 = &uart1;
-		serial1 = &uart0;
+		serial0 = &uart0;
+		serial1 = &uart1;
 		mmc0 = &emmc2;
 		mmc1 = &mmcnr;
 		mmc2 = &sdhost;
@@ -327,35 +328,44 @@
 	status = "okay";
 };
 
+//Set rts for uart0
 &uart0 {
-	pinctrl-0 = <&uart0_pins &bt_pins>;
+	tb-rts-gpio = <17>;
+	pinctrl-names = "default";
+	pinctrl-0 = <&uart0_pins>;
 	status = "okay";
 };
 
+//Set to default
 &uart1 {
+	pinctrl-names = "default";
 	pinctrl-0 = <&uart1_pins>;
-};
+	status = "okay";
+	};
 
 &spi0 {
+	status = "okay";
 	pinctrl-names = "default";
 	pinctrl-0 = <&spi0_pins &spi0_cs_pins>;
 	cs-gpios = <&gpio 8 1>, <&gpio 7 1>;
 
-	spidev0: spidev@0{
-		compatible = "spidev";
-		reg = <0>;	/* CE0 */
-		#address-cells = <1>;
-		#size-cells = <0>;
-		spi-max-frequency = <125000000>;
-	};
+	//Add MCP 2515@0
+	spidev0: mcp2515@0{
+        compatible = "microchip,mcp2515";
+        reg = <0>;      /* CE0 */
+        spi-max-frequency = <10000000>;
+        interrupt-parent = <&gpio>;
+        interrupts = <12 0x2>;
+        clocks = <&can_xtal 0>;
+    };
 
 	spidev1: spidev@1{
-		compatible = "spidev";
-		reg = <1>;	/* CE1 */
-		#address-cells = <1>;
-		#size-cells = <0>;
-		spi-max-frequency = <125000000>;
-	};
+       compatible = "spidev";
+       reg = <1>;      /* CE1 */
+       #address-cells = <1>;
+       #size-cells = <0>;
+       spi-max-frequency = <125000000>;
+    };
 };
 
 &gpio {
@@ -409,8 +419,9 @@
 		brcm,function = <BCM2835_FSEL_GPIO_OUT>;
 	};
 
+	//Set I2C to GPIO 44/45
 	i2c0_pins: i2c0 {
-		brcm,pins = <0 1>;
+		brcm,pins = <44 45>;
 		brcm,function = <BCM2835_FSEL_ALT0>;
 		brcm,pull = <BCM2835_PUD_UP>;
 	};
@@ -463,12 +474,14 @@
 		brcm,pull = <2>;
 	};
 
+	//Set uart0 off
 	uart0_pins: uart0_pins {
 		brcm,pins = <32 33>;
 		brcm,function = <BCM2835_FSEL_ALT3>;
 		brcm,pull = <0 2>;
 	};
 
+	//Set uart1 to pin 32/33
 	uart1_pins: uart1_pins {
 		brcm,pins;
 		brcm,function;
@@ -476,8 +489,8 @@
 	};
 
 	uart2_pins: uart2_pins {
-		brcm,pins = <0 1>;
-		brcm,function = <BCM2835_FSEL_ALT4>;
+		brcm,pins = <32 33>;
+		brcm,function = <2>; /* alt5=UART1 */
 		brcm,pull = <0 2>;
 	};
 
@@ -500,8 +513,47 @@
 	};
 };
 
+//Add sc16is740 and two mcp23008
 &i2c0if {
-	clock-frequency = <100000>;
+    pinctrl-names = "default";
+    pinctrl-0 = <&i2c0_pins>;
+    clock-frequency = <400000>;
+    status = "okay";
+
+    sc16is740@4d {
+        compatible = "nxp,sc16is740";
+        reg = <0x4d>;
+        clocks = <&sc_xtal 0>;
+        interrupt-parent = <&gpio>;
+        interrupts = <16 2>;
+        status = "okay";
+    };
+
+    adc@6c {
+        compatible = "microchip,mcp3424";
+        reg = <0x6c>;
+        status = "okay";
+    };
+ 
+    mcp23008_0@20 {
+        compatible = "microchip,mcp23008";
+        reg = <0x20>;
+        gpio-controller;
+        interrupt-parent = <&gpio>;
+        interrupts = <6 2>;
+        interrupt-controller;
+        microchip,irq-mirror;
+        status = "okay";
+    };
+
+    mcp23008_10: mcp23008_1@21 {
+        compatible = "microchip,mcp23008";
+        reg = <0x21>;
+		#gpio-cells = <2>;
+        gpio-controller;
+        status = "okay";
+		
+    };
 };
 
 &i2c1 {
@@ -510,6 +562,26 @@
 	clock-frequency = <100000>;
 };
 
+&i2c0mux {
+    status = "disabled";
+};
+
+//clocks
+&clocks {
+	sc_xtal: sc16is740_xtal {
+	    compatible = "fixed-clock";
+	    #clock-cells = <1>;
+	    clock-frequency  = <14745600>;
+	    clock-output-names = "sc-osc";
+	};
+	can_xtal: mcp2515_xtal {
+        compatible = "fixed-clock";
+        #clock-cells = <1>;
+        clock-frequency  = <16000000>;
+        clock-output-names = "can0-osc";
+    };
+};
+
 &i2s {
 	pinctrl-names = "default";
 	pinctrl-0 = <&i2s_pins>;
